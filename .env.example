# Generate with: htpasswd -nb admin '<password>'
# When pasted into this file (loaded via docker compose), keep $ as a single $.
# When inlined directly into docker-compose.yml, double $ to $$.
DASHBOARD_AUTH=admin:$apr1$xxxxxxxx$xxxxxxxxxxxxxxxxxxxxxx
